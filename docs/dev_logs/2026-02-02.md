# Development Log — 2026-02-02

## 개요
국가동물보호정보시스템 Tool 실구현, 네이밍 정비, Cat Card DTO 호환 수정, UI 리라이트, 스트리밍 개선 등을 진행.

---

## 완료된 작업

### 1. 국가동물보호정보시스템 Tool 구현

| 항목 | 내용 |
|---|---|
| 기존 | `animal_registry.py` — Mock 데이터 반환, `query_animal_registry(chip_number)` |
| 변경 | `animal_protection.py` — 실제 API 연동, `search_abandoned_animals(sido, sigungu, breed, state)` |

- **`src/agents/tools/animal_protection.py`** (신규)
  - `abandonmentPublicService_v2` API 호출 (`httpx` async)
  - 고양이 전용 (`upkind=422400` 고정)
  - 시도/시군구 코드 → `region_codes.py`에서 변환
  - 최대 5건 반환 (보호소명, 품종명, 나이, 성별, 중성화, 사진, 연락처)
  - 환경변수: `OPENAPI_API_KEY` (공공데이터포털 Decoding 키)
- **`src/agents/tools/region_codes.py`** (신규)
  - 17개 시도 + 전체 시군구 코드 정적 딕셔너리
  - 실제 API(`sido_v2`, `sigungu_v2`)로 확보한 데이터 기반
- **`src/agents/tools/animal_registry.py`** — 삭제

### 2. 네이밍 변경 반영

| 파일 | 변경 |
|---|---|
| `src/agents/liaison.py` | `query_animal_registry` → `search_abandoned_animals` import, 키워드 리스트 업데이트 |
| `src/agents/graph.py` | 동일 import 변경 |
| `docs/01_project/architecture_graph.md` | tool 이름 · 파일경로 반영 |
| `docs/01_project/overview.md` | tool 이름 · 파일경로 · 디렉토리 구조 반영 |
| `docs/01_project/personas.md` | tool 이름 · 파일경로 · 소스코드 링크 반영 |

### 3. Cat Card DTO 호환 수정

- **문제**: `CatCardRecommendation` DTO에 `description`과 `tags`가 required인데, Matchmaker가 `state["recommendations"]`에 넣는 raw MongoDB 문서에는 해당 필드 없음 → Pydantic validation error → `except: pass`로 조용히 실패
- **수정**:
  - `src/agents/matchmaker.py` — `recommendations`에 raw doc 넣을 때 누락 필드 보충:
    - `description` ← `summary[:100]`
    - `tags` ← `personality_traits[:4]` (앞에 `#` 접두어)
  - `src/ui/app.py` — bare `except: pass` → `except Exception as e` + 로그 출력
- **검증**: Mock StoredBreedV3 doc → `CatCardRecommendation(**doc)` 변환 성공 확인

### 4. UserProfile DTO → dict 변환

- **문제**: `app.py`에서 `UserProfile` (Pydantic BaseModel) 객체를 그래프 state에 그대로 전달 → 에이전트들이 `profile.get('housing')` 같은 dict 메서드로 접근 → `AttributeError: 'UserProfile' object has no attribute 'get'`
- **수정**: `src/ui/utils.py` — 그래프 입력 시 `user_profile.model_dump()`로 dict 변환

### 5. UI 리라이트

- **`src/ui/app.py`** — 전면 리라이트
  - Streamlit markdown 위젯 최소화, 인라인 HTML/CSS로 간결하게
  - `header.py` import 제거, 헤더 인라인 통합
  - 로딩 스플래시, 사이드바, 온보딩 폼, 채팅 페이지 정리
  - 리셋 시 `thread_id` 재생성 추가
- **`src/ui/style.css`** — 전면 리라이트
  - Pretendard 폰트 전역 적용 (versioned CDN)
  - Streamlit chrome 숨김, 채팅 메시지 · 입력창 최소 스타일링
  - 과도한 그림자/배경 제거

### 6. 프로필 서랍 UI

- 헤더에 프로필 정보 직접 노출 → `st.expander("📋 집사 프로필")`로 변경
- 클릭하면 서랍 열림: 현재 프로필 값이 selectbox에 반영된 상태로 표시
- 서랍 안에서 바로 수정 후 "프로필 저장" → intro 페이지 이동 없이 채팅 중 프로필 변경 가능

### 7. 스트리밍 개선 (`utils.py`)

- `astream` → `astream_events v2`로 전환 (LLM 토큰 단위 스트리밍)
- 노드별 상태 메시지 (`NODE_STATUS`) 표시: "🎩 집사가 질문을 분석하고 있습니다..." 등
- `get_zipsa_response` (비스트리밍) 함수 제거, 스트리밍 전용으로 단일화
- 기존 `app.astream` + 별도 `app.ainvoke` 이중호출 → `astream_events`에서 `on_chain_end`로 final state 추출하는 단일 호출로 변경

### 8. uvloop 호환 수정

- **문제**: `nest_asyncio.apply()`가 uvloop 이벤트 루프와 비호환 → `ValueError: Can't patch loop of type <class 'uvloop.Loop'>`
- **수정**: `nest_asyncio` 제거, uvloop 감지 시 `asyncio.DefaultEventLoopPolicy()`로 전환

---

## 변경 파일 전체 목록

| 파일 | 유형 | 설명 |
|---|---|---|
| `src/agents/tools/animal_protection.py` | **NEW** | 유기동물 조회 Tool (실제 API) |
| `src/agents/tools/region_codes.py` | **NEW** | 시도/시군구 코드 딕셔너리 |
| `src/agents/tools/animal_registry.py` | **DELETE** | Mock tool 제거 |
| `src/agents/liaison.py` | MODIFY | import 변경, 키워드 리스트 업데이트 |
| `src/agents/graph.py` | MODIFY | import 변경 |
| `src/agents/matchmaker.py` | MODIFY | recommendations에 description/tags 보충 |
| `src/ui/app.py` | **REWRITE** | UI 전면 리라이트 + 프로필 서랍 + uvloop 대응 |
| `src/ui/style.css` | **REWRITE** | 미니멀 스타일 전면 재작성 |
| `src/ui/utils.py` | **REWRITE** | astream_events v2 스트리밍, 노드 상태 표시 |
| `docs/01_project/architecture_graph.md` | MODIFY | 네이밍 반영 |
| `docs/01_project/overview.md` | MODIFY | 네이밍 반영 |
| `docs/01_project/personas.md` | MODIFY | 네이밍 반영 |

---

## 환경변수 추가

| 키 | 용도 | 비고 |
|---|---|---|
| `OPENAPI_API_KEY` | 공공데이터포털 API (Decoding 키) | `.env`에 추가 필요 |

---

## 남은 작업 / 체크리스트

### 필수

- [ ] **region_codes.py 정합성 재검증** — 경상남도 `창원시`가 API에서 3개 코드(5280000, 5320000, 5670000)로 반환됨. 현재 첫 번째(5280000)만 저장. 필요 시 중복 키 처리 로직 추가.
- [ ] **Liaison 통합 테스트** — 실제 채팅 흐름에서 "강남구 근처 입양 가능한 고양이 보여줘" → Liaison 라우팅 → `search_abandoned_animals` tool call → 결과 텍스트 응답 확인
- [ ] **Matchmaker Cat Card 통합 테스트** — 품종 추천 요청 → 우측 패널에 카드 정상 렌더링 확인 (description/tags 필드 포함)
- [ ] **스트리밍 토큰 필터링 검증** — `astream_events`에서 head_butler의 routing 단계 structured output 토큰이 사용자에게 노출되지 않는지 확인. 현재는 "content가 있는 모든 토큰"을 내보내므로, routing LLM이 content를 생성하면 노이즈 발생 가능.

### 개선 사항

- [ ] **유기동물 결과 UI 포맷** — 현재 Liaison은 텍스트로만 응답. 보호소 사진, 연락처 등을 카드 형태나 구조화된 텍스트로 표시하면 가독성 향상.
- [ ] **품종 코드(`kindCd`) → 품종명 매핑** — 현재 `kindNm` 필드 사용 중이나, 특정 응답에서 `kindNm` 누락 시 fallback으로 코드-품종 매핑 테이블 필요 여부 확인
- [ ] **breed 파라미터 필터링 개선** — `search_abandoned_animals`에서 `breed` 필터가 단순 `in` 검색. 형태소 분석이나 유사어 매핑 적용 검토.
- [ ] **에러 핸들링 강화** — API 키 미설정, 네트워크 타임아웃, rate limit 시 사용자 친화적 메시지 표시

### 기술 부채

- [ ] 테스트 스위트 부재 — 주요 함수(`search_abandoned_animals`, DTO 변환, 프로필 직렬화)에 대한 최소한의 단위 테스트 작성 검토
